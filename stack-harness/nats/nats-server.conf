host: 0.0.0.0
port: 4222
jetstream { store_dir: /data }
# Mutual TLS via the harness CA — mirrors the real nats role (verify:true).
tls {
  cert_file: /certs/server.crt
  key_file:  /certs/server.key
  ca_file:   /certs/ca.crt
  verify:    true
}
