---
# Profile: base (default). No overrides — group_vars/all.yml and role
# defaults apply as-is. SSH accepts from any IPv6 source (bastion behaviour;
# also required on a fresh node before the mesh exists).
{}
